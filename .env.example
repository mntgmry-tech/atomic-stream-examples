# =============================================================================
# AtomicStream x402 Client Configuration
# =============================================================================

# Your Solana wallet private key (base58 encoded)
# This wallet will be used to sign x402 payment transactions
SVM_PRIVATE_KEY=your_base58_encoded_private_key_here

# AtomicStream API base URL (default: https://x402.atomicstream.net)
# PUBLIC_HTTP_BASE_URL=https://x402.atomicstream.net

# Stream schema path - choose from available streams:
#   /v1/schema/stream/mempool-sniff      - Mempool Transaction Stream ($3.00/5min)
#   /v1/schema/stream/new-mints          - New Token Mint Feed ($1.50/5min)
#   /v1/schema/stream/whale-alert        - Large Transfer Alerts ($0.60/5min)
#   /v1/schema/stream/smart-money        - Wallet Activity Watchlist ($0.60/5min)
#   /v1/schema/stream/wallet-balance     - Wallet Balance Updates ($0.15/5min)
#   /v1/schema/stream/token-ticker       - DEX Price Ticks ($0.15/5min)
#   /v1/schema/stream/liquidity-changes  - Liquidity Change Stream ($0.60/5min)
#   /v1/schema/stream/infrastructure-pulse - Network Pulse ($0.03/5min)
#   /v1/schema/stream/sniper-feed        - New Pool Early Swaps ($3.00/5min)
#   /v1/schema/stream/rug-detection      - Rug Risk Alerts ($3.00/5min)
#   /v1/schema/stream/market-depth       - Pool Vault Updates ($0.60/5min)
#   /v1/schema/stream/token2022-extensions - Token-2022 Extension Events ($0.60/5min)
#   /v1/schema/stream/program-logs       - Program Log Stream ($0.15/5min)
#   /v1/schema/stream/program-errors     - Program Error Stream ($0.15/5min)
#   /v1/schema/stream/trending-leaderboard - Trending Tokens Leaderboard ($0.15/5min)
#   /v1/schema/stream/account-data       - Account Data Stream ($0.15/5min)
X402_SCHEMA_PATH=/v1/schema/stream/mempool-sniff

# Renewal method: 'http' (default) or 'inband'
# RENEW_METHOD=http

# =============================================================================
# Stream Filters (comma-separated lists)
# =============================================================================

# Accounts to watch (required for some streams like mempool-sniff, smart-money)
# Example: WATCH_ACCOUNTS=4vJ9JU1bJJE96FWSJKvHsmmFADCg4gpZQff4P3bkLKi,8qbHbw2BbbTHBW1sbeqakYXVKRQM8Ne7pLK7m6CVfeR
WATCH_ACCOUNTS=

# Programs to watch (alternative to accounts for mempool-sniff)
# Example: WATCH_PROGRAMS=675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8
WATCH_PROGRAMS=

# =============================================================================
# Output Options
# =============================================================================

# Event format: 'enhanced' (default) or 'raw'
EVENT_FORMAT=enhanced

# Include account keys in transaction events
INCLUDE_ACCOUNTS=true

# Include token balance changes
INCLUDE_TOKEN_BALANCE_CHANGES=true

# Include transaction logs
INCLUDE_LOGS=false

# Include parsed instructions
INCLUDE_INSTRUCTIONS=false

# Filter token balances to specific mints
FILTER_TOKEN_BALANCES=false

# Transaction logging mode: 'full' (default) or 'summary'
LOG_TRANSACTIONS=full

# Log level: 'debug', 'info' (default), 'warn', 'error'
LOG_LEVEL=info
